# ---- Core libraries your agent can actually use ----
pytest~=8.0          # test runner Codex can call
requests~=2.32       # handy HTTP client for demos
rich~=13.7           # nicer CLI output (optional)
gensim~=4.3          # word embedding models for the NLP notebook
nltk~=3.9            # tokenization and stopword lists
matplotlib~=3.9      # plotting for embedding visualizations
scikit-learn~=1.5    # PCA dimensionality reduction for embeddings
